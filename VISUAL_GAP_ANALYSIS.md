# VISUAL_GAP_ANALYSIS — 当前实现 vs Reference 原型图（v2）

> 基准：`docs/prototypes/agv-3d-scene-prototype.png`（Reference）
> 现状：`pnpm dev`（localhost:5173）实机截图，fov 60 / 45° 取景 / mock 数据源（默认 60 台车）
> 性质：**纯视觉差距分析，不含任何代码修改**。本版为 v2，按评审结论修正了 v1 中的事实错误（阴影未启用、独占区几何内容、标签/车队数量与比例）、尺寸计算错误（相机占比目标、标签尺寸）与实现复杂度低估（路口接缝、仓库货架、Bloom 前置条件），并显式区分「纯外观修改」与「需要语义/算法决策的前置项」。
> v1 中经复核确认无误的判断（数据规模、相机距离与占比测算、尺度倒置、文字海、标签底色机制、灯光参数）全部保留。

---

## 0. 一句话结论

当前实现是一个**正确的工程骨架 + 错误的视觉尺度系**：地图在画面里只占约 50%、被纯黑包围；4291 个节点圆盘（直径 0.7m）比路面（0.5m）还宽，层级倒置，形成"点阵地毯"；1185 个仓库节点名称在中景泛滥成黄色文字海；车辆标签用状态色做底色形成大片亮蓝色芯片；方向光 2.2 过曝把本应饱和的车体色（受光材质）洗成粉白。**其中大部分是外观常量与几何层的问题；少数项（仓库聚合、独占区面填充）超出外观范畴，需要语义/算法层的前置决策，本文已单独标出。**

一个必须先说清的客观事实：Reference 是一张精心摆拍的小场景——画面中可见 **8 台车辆**（6 台带标签：AGV-017/008/023/031×2/009，另有绿色、灰色 2 台无标签）、约 20 个节点、4 处仓库区；且**右下角有一个图例面板**（路径/节点/充电站/仓库/停车点/独占区/交通锁）。而真实地图是 **239m × 126m、4291 节点、9265 条逻辑边、59 个充电节点**的密集网络（`json/map.json`）。视觉逼近的目标不是"把 4291 个节点拍得像 20 个"，而是：①总览层级读得清路网骨架，②近景层级有 Reference 的材质与色彩质感。
**关于图例面板**：SPEC（`docs/SPEC_20260901_agv-3d-monitor.md` §7.1）明确约定 Canvas 外无任何 DOM 覆盖层，图例属产品范围外，本文**主动忽略**该项，不列入视觉差距；后续若产品范围变更应另立需求，不得在本次视觉工作中静默加入。

---

## 1. Camera

现状代码：
- `src/app/App.tsx:328` — `camera={{ fov: 60, near: 0.5, far: 4000, position: [0, 300, 300] }}`
- `src/features/camera-navigation/model/overviewFraming.ts:46-81` — 45° 俯角 + 45° 方位角，距离 = 包围球包络 `（对角线/2)/tan(fov/2) × 1.1`
- SPEC §5.5 约定「俯视角约 45°」——**俯角本身不是问题，问题在视场角与取景算法**。

### 1.1 P0 — 取景按包围球计算，地图在 viewport 中只占 ~50%（构图第一杀手）

地图对角线 ≈ 271m（239×126 矩形）。包围球半径 135.5m 决定了包络距离（fov60 时 ≈ 258m），但 45° 方位角下矩形地图在屏幕上的投影足迹只有约 259m × 183m——包围球把一个"长方形"当成"球"来包络，四周留出大量空气。实测首屏截图：地图宽度占画面 **~49%**、高度 **~61%**，其余全是纯黑。

**必须修正的表述（v1 错误）**：v1 称"替换算法不影响任何调用方"——**不成立**。四角投影取景必须使用相机 aspect，而 `computeOverviewPose(bounds, fovDeg)` 是二元纯函数，调用处 `useCameraNavigation.ts:141` 也未传视口比例。改为按四角投影取景时函数签名必须扩展为 `(bounds, fov, aspect)`（或传入 `gl` 尺寸），调用方同步修改。改动仍属局部（一个纯函数 + 一处调用），但不是零签名变更。

**占比目标的定义（v1 含糊）**：在 45° 方位角、16:9 视口下，地图投影足迹长宽比 ≈ 1.41，小于视口的 1.78——**高度是约束方向**。若完整显示地图并留 10% 边距，可达 **高 ~90%、宽 ~72%**；两个方向同时 ≥85% 在不改变方位角、不裁切的前提下**数学上不可能**。因此目标明确定义为：
- **主指标：高度占比 ≥ 90%**（宽度随之 ~72%），四角投影 + 10% 余量；
- 可选增强：按包围盒长宽比微调方位角（如 40°），使投影足迹长宽比接近视口比例，双向占比趋向均衡——是否采用需单独验证取景效果后再定，不作为默认方案。

### 1.2 P0 — fov 60 是广角无人机视角，Reference 是长焦/弱透视视角

注意一个反直觉的点：**在包围球取景下，改 fov 几乎不改变地图占比**（距离和可视高度同比例缩放），fov 真正决定的是**透视强度**。fov 60、距离 258m 时，近处路面与远处路面的透视收缩差异极大（首屏截图中地图呈明显的梯形），而 Reference 中所有平行道路在屏幕上近乎平行、只有极弱的近大远小——这是 **fov 20~30 的长焦机位**（等价距离拉远 2~3 倍）的观感。

**建议**：总览机位 fov 降到 **25°±5**（相应取景距离 ×2.3 左右，near/far 与 maxDistance 同步放大），俯角保持 40~45°、方位角保持 45°（Reference 的路网走向与当前一致）。**不建议直接换 OrthographicCamera**：OrbitControls 的 dolly/跟随偏移/滚轮缩放全部按透视距离建模（`useCameraNavigation.ts` 的跟随缩放、min/maxDistance 恒等式），换正交是伤筋动骨的重构且收益与长焦透视几乎相同——长焦透视是本项目的"弱透视"正确实现路径。

### 1.3 P1 — near/far 与缩放上限需随 fov 联动

fov 降到 25° 后包络距离约 600m，`overviewFraming.ts` 的 `maxDistance = 对角线×3 ≈ 813m`、`far = 对角线×6 ≈ 1628m` 仍然够用，但 `App.tsx` 初始 `position [0,300,300]`（距原点 424m、高度 300m）在 bounds 就绪前的一瞬会以完全不同的透视闪一帧——建议初始 position 改为与新 fov 匹配的同方向机位。`near 0.5` 可保留。

---

## 2. Scene Composition（构图与地图占比）

### 2.1 P0 — "黑色孤岛"：地坪只外扩 10m，之外是纯黑虚空

`GROUND_MARGIN_M = 10`（`mapAppearance.ts:32`）相对 271m 的地图是 4% 的边距，加上取景留白，画面里地坪是一块悬在纯黑（`MAP_CLEAR_COLOR #0c0e12`）中的孤岛。Reference 中**地面延伸出画面之外**，背景 `#161b22~#1b1e26`（实测采样）与地面 `#242931` 同属一个色域，两者靠雾感/暗角融合，不存在"边界"。

**建议**（三选二即可）：
1. 取景占比提升（1.1）后，地坪边缘自然出画（覆盖大半问题）；
2. `GROUND_MARGIN_M` 提到 **40~60m**，并在 `MAP_CLEAR_COLOR` 提亮为 `#14181f` 左右向地面色靠拢；
3. 加 `scene.fog`（`FogExp2`，密度按对角线调，使远处地面渐隐进背景色）——所有现有材质（含 MeshBasicMaterial 默认 `fog:true`）自动支持，成本为零。

### 2.2 P1 — 场景中心与朝向

取景目标恒为包围盒中心（正确），方位角 45° 使矩形地图呈菱形、四角留黑。占比提升后此问题弱化；若仍觉松散，可按 1.1 的可选增强调整方位角，属纯函数改动。优先级低于 1.1/2.1。

### 2.3 P1 — 节点密度与"总览可读性"（数据现实的应对）

总览距离 300~600m 下，单个节点盘投影 < 4px，4291 个节点叠加成发光点毯，路网结构反而不可见（首屏截图）。Reference 的总览之所以可读，是因为它只有 20 个节点。
**建议（修正 v1 的实现描述）**：v1 所写"实例矩阵逐帧零缩放"**不可取**——节点实例缓冲是 `StaticDrawUsage` 一次上载的静态设计（`NodesLayer.tsx` 不变量 2），逐帧改写破坏该不变量且引入每帧 CPU 开销。正确路径是 **shader LOD**：在节点材质中注入「投影尺寸 < 阈值 → alpha 淡出/顶点收缩」（基于 `gl_Position` 推导屏幕尺寸，纯 GPU、零逐帧写入）。注意这是新增着色器注入 + 与质量能力分级（TASK-014 的能力映射）接线的组合改动，**不是外观常量修改**，实施成本按"小特性"评估而非"改个数值"。

---

## 3. Ground

现状：`GROUND_COLOR #16191f`，roughness 0.95 / metalness 0.05，5m 间隔网格线 `#232830`（`mapAppearance.ts:27-36`，`GroundLayer.tsx`）。

### 3.1 P1 — 地面偏黑，与 Reference 的"深灰蓝"差一档

Reference 实测：地面 `#242931`、背景 `#161b22`；当前：地面 `#16191f`、背景 `#0c0e12`。当前整体暗一档半，导致深灰路面（`#363c45`）与地面的明度对比不足，路面"陷"进地里。
**建议**：`GROUND_COLOR → #1e232b` 左右、`MAP_CLEAR_COLOR → #14181f` 左右，保持"路面比地面亮、地面比背景亮"的 Reference 明度阶梯（路 `#494c54` > 地 `#242931` > 背景 `#161b22`）。roughness 0.95 / metalness 0.05 合理，可保留。

### 3.2 P1 — 网格：5m 线框在总览是"方格纸"，在近景却不存在

当前 5m LineBasicMaterial 在总览把地坪切成醒目方格（首屏截图非常明显），近景时又因线宽 1px 几乎不可见——两个距离下都是负作用。Reference 的地面是**细密、低对比的工业地坪纹理**（细网格 + 分缝），从不喧宾夺主。
**建议**：网格线颜色压暗到 `#1b2028`（与地面明度差 ≤ 8%），或改为 Canvas 纹理（细线 1m + 粗分缝 5m 一张平铺贴图），同时解决"总览太抢、近景没有"。这是纯外观替换，`GroundLayer` 结构不变。

### 3.3 P2 — 地面喧宾夺主判定

当前真正"喧宾夺主"的不是 roughness/metalness（数值合理），而是 3.2 的网格对比度与 2.1 的孤岛感。这两个解决后地面回归"背景板"角色。

---

## 4. Routes（路径）

现状：0.5m 宽扁平条带 `MeshBasicMaterial #363c45` + 1px 中线 `#4b525c`（`mapAppearance.ts:52-55`）；几何为**逐段独立四边形**，无斜接处理（`buildMapGeometry.ts:220-256`，注意循环按**每条物理路径**分别展开）。

### 4.1 P0 — 为什么"看起来像线"：宽度倒置 + 接缝破碎（含路口）

两个原因叠加：
1. **层级倒置**：路面宽 0.5m，而节点盘直径 0.7m——节点比路还宽。加上车宽 0.7m（`agvDimension`，默认车长 1.8m）也比路宽，Reference 中"路 > 车 > 节点点"的包含关系完全崩塌。路失去了"面"的感觉，视觉上退化为连接节点的"线"。
2. **接缝破碎有两层，v1 只说了第一层**：
   - **路径内**：同一条贝塞尔路径的相邻段各自用法线展开，弯道处双重覆盖与毛边（近景截图可见）——共享顶点 polyline strip（相邻段法线平均 + miter 长度补偿）可修复；
   - **路口处**：不同逻辑边（不同物理路径）在节点处相接，各自独立生成几何——**路径内方案管不到这里**。把道路加宽到 2m 后，路口会变成多片 2m 宽条带的叠片与尖角缺口。必须补**路口处理**：最小方案是每条物理路径的端点加**方形/圆形端帽**（端点处补一块半径 = 半路宽的圆片，恰好盖住接缝，几何增量小、无需拓扑分析）；彻底方案是按节点聚合邻接路径做圆角连接或平面并集（涉及拓扑链合并，复杂度高）。
   - **注意**：节点盘（r≈0.25m）远小于加宽后的半路宽（0.8~1.0m），**不能指望节点圆盘遮住路口接缝**——端帽不可省略。

**建议**：
- `PATH_SURFACE_WIDTH_M` 0.5 → **1.6~2.0m**（≥ 2× 车宽），`NODE_RADIUS_M` 0.35 → **0.22~0.28m**（≤ 0.5× 路宽），恢复 Reference 的尺度链；
- 条带展开改为共享顶点 strip + **路径端点圆片端帽**（最小完备方案）；弯道圆滑度由既有的 24 段贝塞尔采样保证；路口圆角/平面并集留作后续增强，不阻塞本次；
- **不推荐 Line2/LineMaterial**（linewidth 依赖解析器、与合批架构冲突）、**不推荐 TubeGeometry**（5 万段管子纯粹浪费）。改动集中在 `buildMapGeometry.ts` 的条带生成循环，输出契约（`pathsSurface`/`pathsCenterline` 两个 BufferGeometry）不变。

### 4.2 P1 — 路面的"厚度感"与边缘线

Reference 的路面有清晰的**亮色描边 + 微弱立体感**（浅灰面 + 深色侧缘，像略高于地面的铺装板）。当前是单色平面贴花。
**建议**（低成本近似）：条带两侧各挤一条 3~5cm 的深色边线（顶点色或第二组合批 LineSegments，颜色取 `#1a1e24`），路面中心色提亮到 `#3f444d`；中线改为**虚线段**（中心线生成时按 1m 实 0.6m 空断开，纯几何改动）。这给出 Reference 的"道路是构件"的观感，无需真实挤出厚度。

### 4.3 P2 — 车流方向箭头

Reference 中路面上的浅蓝导向箭头是**车行方向指示**（跟随车辆生成）。当前车辆前方已有一个浅色三角贴片（截图可见），与 Reference 的表达已部分对应，暂不需要新增；如要更像，可把该贴片改为沿路径方向的细长箭头形。P2。

---

## 5. Nodes（节点）

现状：单个 InstancedMesh，`CircleGeometry r=0.35m、12 段`，MeshBasicMaterial + instanceColor（`NodesLayer.tsx`，`mapAppearance.ts:57-59`）。

### 5.1 P0 — 尺寸与权重：节点是画面第一主角，Reference 里它是最小元素

0.7m 的圆盘 + 高饱和青/黄色 + Unlit 材质，使 3045 个 work 节点 + 1185 个 warehouse 节点成为整个画面的视觉主体（近景截图中节点盘盖住路口、车辆在"圆点海"里游泳）。Reference 中节点是**嵌在路口里的小圆点**：直径约为路面宽的 30~40%、带暗色描边、亮度克制，视觉权重远低于路面与车辆。

**建议**：
- 半径降到 0.22~0.28m（见 4.1），段数 12 → 20（近景 12 边形轮廓已可辨认，截图里多边形边缘明显）；
- 颜色保留现有 NODE_COLORS 色相（与 Reference 色系一致），但 work 节点降一档饱和度（`#3fb3a4 → #35948a` 左右），charge/park/warehouse 保持语义跳色；
- 每个盘加一圈 1~2px 暗描边（图集贴图或几何内环），Reference 的节点有清晰的"嵌 into 路面"轮廓。

### 5.2 P1 — 总览剔除（同 2.3）

shader LOD 屏幕尺寸淡出（实现口径与成本见 2.3 的修正说明），让总览回归路网骨架。此项与 2.3 是同一件事的两面，实施时任选其一处统一实现。

---

## 6. Vehicles（车辆）

现状：全程序化 7 部件（底盘盒/外壳盒/方向楔/平台/托盘/信标/假阴影），外壳 MeshStandardMaterial + 实例状态色（`createVehicleGeometry.ts`，`fleetAppearance.ts:41-101`）。Mock 默认车队 **60 台**（`MockVehicleDataSource.ts` `MOCK_DEFAULT_VEHICLE_COUNT`），压力场景 250 台。

### 6.1 P0 — 受光材质被"洗白"：方向光过曝 + ACES 把状态色变粉彩

IDLE 蓝 `#4f8dff` 的车在近景截图中呈**近白粉蓝**，FAULT 红/EXECUTING 绿同理会失真。原因：`DIRECTIONAL_LIGHT_INTENSITY = 2.2` + RoomEnvironment IBL + ACESFilmic（默认 exposure 1）叠加，受光漫反射面在亮度上溢出后 ACES 去饱和。Reference 中车体是**深底色上高饱和的色块**（红是 `#c33` 级别的深红、蓝是宝蓝），且车底/车侧有深色结构承托。

**必须修正的表述（v1 错误）**：v1 把「降方向光强度」与「降 toneMappingExposure」当作等效替代——**不等效，且有本质区别**：
- **降方向光强度**（2.2 → 1.1~1.3）：只影响**受光材质**（车体、底盘、地坪）。节点、标签、路面等 MeshBasicMaterial（Unlit）**完全不受影响**，饱和度不动——这是**推荐路径**；
- **降 toneMappingExposure**：作用于**全画面最终色调映射**，节点、标签、道路等 Unlit 元素会一起变暗变灰——v1 中"或 exposure 0.85"的写法会误伤 Unlit 贴花层，**删除该替代项**（除非后续单独做分级曝光，那属于新特性）。

**建议**：`DIRECTIONAL_LIGHT_INTENSITY 2.2 → 1.1~1.3`，IBL 保留，exposure 不动。改完车体、地坪的饱和度回血，Unlit 层不受影响。这是受光材质色彩质感的总开关，**先调它再评判其它颜色**。

### 6.2 P1 — 与 Reference AGV 的形态差距

Reference AGV 可辨认出：深色底盘 + **可见车轮**、饱和色外壳、车顶载货（纸箱/货架）、顶灯。当前程序化车有底盘/外壳/楔/托盘/信标，缺车轮与立体层次，且比例偏"肥皂"。
**建议**：
- **不必上 GLTF**：车的部件全部可程序化补齐——4 个圆柱车轮（merge 进 chassis 几何或独立 InstancedMesh）、外壳顶面留深色边框（顶/侧双色，用两个盒子或顶点色）、托盘上叠 1~2 个纸箱色小盒表达载货。现有 7 部件 InstancedMesh 架构直接扩展 1~2 个 part kind 即可，Draw Call 预算增量可控；
- GLTF/GLB 仅在后续需要"原型同款车型"时引入（单模型 ≤ 5k 三角、KTX2 贴图），当前阶段收益低于成本；
- 车体色映射 `SHELL_STATE_COLORS` 色相本身与 Reference 一致（红/蓝/黄/绿/紫），过曝解决后无需改表。

### 6.3 P1 — 车与路的尺度

车宽 0.7m > 路宽 0.5m（真实数据 vs 外观常量的矛盾）。路加宽到 1.6~2.0m 后（4.1）此矛盾自动消解，**不要缩车**——车是近景主角，Reference 中车占路宽约 50~60%，加宽路面即达成。

---

## 7. Labels（标签）

### 7.1 P0 — 1185 个仓库名称平铺在地面：中景的"黄色文字海"

`buildLandmarkData.ts:94-99` 为**每个 warehouse 节点**生成地面名称锚点（`WAREHOUSE_NAME_HEIGHT_M=0.9` 的四边形），1185 个黄色 "JYT-DROP-xxx" 贴满中景画面（截图 2/3 触目惊心）。Reference 里**根本不存在仓库名称文字**——仓库只有米色垫 + 货架，唯一的区域文字是"独占区 1/2"。
**建议**：仓库节点名称从地面层**整体移除**（或 fade 区间收紧到 ≤ 15m 的拾取辅助级别）；保留独占区名称（Reference 有）与停车 P 字形。改的是 `LandmarksLayer` 的名称输入组装，图集/淡出材质机制原样复用。此项属纯外观层修改，无语义影响（节点本身、拾取、业务逻辑均不动）。

### 7.2 P0 — 车辆标签底色用状态色：亮蓝色芯片群落（数量与尺寸数据已修正）

**修正 v1 的数字错误**：Mock 默认 **60 台**（不是 v1 所写 200 台）；默认车长 **1.8m**（`vehicle.json`），标签宽 2.4m = **车长的 1.33 倍**（不是 v1 所写"比车大 2 倍"）。问题的主要来源因此不是"尺寸巨大"，而是**底色 + 数量 + 亮度**：
- `labelMaterials.ts:216` 背景取 `状态色 × 0.72`，60 台 IDLE 车就是 60 个亮蓝芯片；状态色满底使大面积高饱和色块出现在画面上层；
- Reference 标签是**深灰黑底 `#1a1f26` + 白色 ID + 左上角状态色小圆点 + 底部电量条**——状态信息由"点"承载，不由"底"承载。

**建议**：
- 背景色改深灰底；状态色改为左上角圆点 + 既有 L1/L2 描边（告警语义保留）。**实现成本修正（v1 低估）**：圆点不是"改一处颜色常量"——需要在背景层 shader 中新增 SDF 圆绘制（复用现有 `aStateColor` 实例属性传色）或在状态芯片图集（`labelAtlas.ts` 的 Canvas 绘制流程）中新增圆点单元格，属**小段 shader/图集开发**，工作量按半天级评估；
- 尺寸：Reference 中芯片宽 ≈ 车长的 **1.1~1.3 倍**（视觉实测 AGV-017 芯片 94px vs 车长 85px ≈ 1.1×）。当前 1.33× 略大但非主因，建议温和下调 `LABEL_HEIGHT_M 0.6 → 0.45~0.5`（宽 1.8~2.0m = 车长 1.0~1.1×），**不要**按 v1 建议砍到 1.3~1.5m（仅车长 0.72~0.83 倍，会小于 Reference 比例，文字可读性受损）；
- 现有 8px/20px LOD 与重点 20 个上限的骨架正确，保留。

### 7.3 P1 — LOD 策略评估

现状策略（投影 ≥8px 全名 / ≥20px 完整 / 远景 20 个重点）方向正确，问题只在世界尺寸（7.2）与总览无"重点之外全隐"的强冷寂档。建议总览档（投影 < 8px）只保留 selected + FAULT 两类（现有 `labelImportanceRank` 秩 0/1，改一处过滤即可），总览画面的芯片数量从"所有重点"降到"告警本体"。

---

## 8. Regions（区域语义层）

| 区域 | Reference | 现状 | 等级 |
|---|---|---|---|
| 独占区 | 半透明蓝色**面填充** + 发光描边 + 名称，实测填充 `#37465e` | 仅 0.9m 加宽条带（opacity 0.3），**无面填充** | P1（需新算法，见 8.1） |
| 交通锁 | 大面积半透明红/黄**抬升面板**（"已锁定"/"申请中"覆盖整个路口，边缘发光） | 贴地四边形 opacity 0.4，读作"路面变色" | P1（不改业务形状，见 8.2） |
| 仓库 | 米色垫 + **3D 货架与纸箱** | 半透明黄垫（无货架）；名称泛滥见 7.1 | **需语义决策，见 8.3** |
| 停车点 | 紫色凸起 slab + 大白 P + 微光 | 紫垫 + 白 P 字形（形态已对，缺凸起感与光晕） | P2 |
| 充电站 | 青色立柱 + 闪电符号 + 发光底环（当前最接近 Reference 的元素） | 立柱 + 底环 + 呼吸灯球（缺符号贴图/光晕）；**全图 59 个充电节点已形成成排立柱** | P2（含密度问题，见 8.4） |

### 8.1 独占区面填充（P1，但实现依据已修正）

**修正 v1 的事实错误**：`buildExclusiveGroupsGeometry.ts:102-134` **并未**产出"成员边包围区域"——它只把成员物理路径按 `EXCLUSIVE_OUTLINE_WIDTH_M` 做与路面相同的条带展开（只是更宽），**没有任何区域多边形、轮廓提取或填充面**。要实现 Reference 的整块半透明蓝色区域，必须新增空间算法，可选路线（按成本升序）：
1. **成员节点 AABB/旋转包围盒填充**（最简，成本最低，但形状与路网不贴合）；
2. **成员路径线集的凸包**（凸包算法简单可靠；独占区多为环形路网，凸包会盖住外部空地，视觉可接受度需验证）；
3. **路径并集/凹包 + 内孔**（最贴合 Reference 观感，实现复杂度高，属几何算法特性开发）。
无论哪条路线，都是在 `buildExclusiveGroupsGeometry` 内**新增一个填充几何输出**（保持现有 outline 不变），不是 v1 所说"补一个 Mesh"那么轻。此项**从 P1 调整为 P1（前置：先由产品确认形状贴合度要求）**，若只求"有一块蓝"可用路线 1/2 快速验证。

### 8.2 交通锁（P1，不放大业务形状）

**修正 v1 的方案错误**：交通锁矩形是调度系统上报的**真实闭锁/申请范围**（`vehicle.json` 可见约 1.8×0.7m 的扫掠矩形），**放大 1.5~2× 会歪曲业务语义**，不可取。改为保持原形状，仅增强表达：
- 抬升 0.15~0.25m（Reference 的面板悬浮感）；
- 边框亮线（locked 红 / applying 黄描边，与标签告警配色同源）；
- 透明度 0.4 → 0.5；
- 面板内加"已锁定/申请中"小字（复用标签图集机制；Reference 中明确有此文字）。
- 抬升后注意与标签 renderOrder 的既有分层约定（`fleetAppearance.ts` 不变量 6），加一层高度常量即可。

### 8.3 仓库货架（从 P1 移出：需语义聚合决策）

**修正 v1 的可行性错误**：仓库类别是 **1185 个取放站点**，`buildLandmarkData.ts` 按节点逐一生成垫与锚点。v1 建议"每仓库垫上放货架"照此实施 = **1185 组货架**：遮挡道路、形成新的模型海、且毫无"仓库区"的聚合感——与 Reference 的"4 处大仓库区"形态南辕北辙。
正确路径需要**语义聚合**（把站点聚合为少量物理区域，或引入独立的仓库 POI 配置），这已经**超出外观常量与纯视觉修改的边界**，涉及数据模型/配置决策。处理方式二选一：
- **A（推荐）**：本次视觉工作不动货架，仅完成 7.1（移除名称）+ 垫子配色微调（降低透明度、改米色），仓库区观感提升到"干净"；
- **B**：单独立项做站点→区域聚合（输入：站点坐标聚类或显式分组配置），聚合后再按区域放置货架。该项列为**前置决策项**，不进 P0/P1 实施清单。

### 8.4 充电站密度（P2，含密度问题）

全图 **59 个充电节点**，页面中已出现成排立柱（截图 2 顶部可见 8 连排）。v1 只讨论了单体造型。补充：单体增强（闪电贴花 + 底环脉冲）会同时放大 59 处的视觉噪音，若做增强需与总览 LOD（2.3 同款 shader 淡出）联动，避免"59 个发光体"总览抢戏。优先级 P2。

### 8.5 停车点（P2）

P 字 slab 抬升 3~5cm + 微发光，形态已对，纯外观微调。

---

## 9. Lighting

现状：单 DirectionalLight 2.2 白光 + RoomEnvironment/PMREM IBL；无 Ambient/Hemisphere；ACESFilmic exposure 1（`App.tsx:327`）。**修正 v1 的事实错误：阴影当前完全没有生效**——`App.tsx` 的 Canvas **未传 `shadows` prop**（R3F 默认 `shadows=false`，即 `gl.shadowMap.enabled=false`），代码里设置的 `castShadow`、阴影相机与 2048 分辨率**全部空转**，场景中不存在任何阴影贴图。近景中车辆下方那块暗色是程序化假阴影贴片（`VEHICLE_SHADOW_*`），不是实时阴影。

### 9.1 P0 — 受光材质过曝（同 6.1）

`DIRECTIONAL_LIGHT_INTENSITY 2.2 → 1.1~1.3`。注意与 exposure 的区别（见 6.1 修正说明）：只降方向光，不动 exposure，保护 Unlit 贴花层。

### 9.2 P0 — 阴影从未启用，需先"开"再"调"（重写 v1 P1-11）

启用与调优是两步，顺序不能颠倒：
1. **启用**：`App.tsx` Canvas 加 `shadows` prop（R3F 下 `shadows` 等价 `gl.shadowMap.enabled = true` + 默认 `PCFSoftShadowMap`）。此改动同时影响渲染负载与 TASK-016 资源代重建范围（阴影贴图属 GPU 资源），需按既有恢复链验证；
2. **收紧阴影相机**：当前 `createStaticDirectionalLight`（`MapVisualizationFeature.tsx:283-317`）用的是 `half = 对角线/2 + margin` 的**正方形**，不是地图 AABB——对 239×126 的地图覆盖 311m 见方，其中近半面积在地图之外。**仅把 margin 20→4 只从 311m 降到 279m，改善有限**。正确做法是按**灯光空间下的地图四角**（AABB 四角变换到光空间后取 min/max）设置 left/right/top/bottom，覆盖宽度约 250×140m，有效分辨率提升 ~2.5 倍（同 2048 贴图下 ~9cm/texel）；
3. **柔化**：R3F `shadows` 默认即 PCFSoftShadowMap；注意 **`shadow.radius` 在 PCFSoftShadowMap 下不生效**（three.js 仅在 PCFShadowMap / VSMShadowMap 下使用 radius）。若要 Reference 那种大范围软影，选型是：PCFSoft（默认、稳定、不可调 blur）或 VSMShadowMap + `radius/blurSamples`（可调、有 light bleeding 与额外 blur pass 成本）。**建议先用 PCFSoft + 收紧后的相机验证观感**，VSM 仅在仍偏硬时作为增强项评估；
4. 车辆假阴影贴片保留（Reference 同样是"假接触阴影"观感），启用实时阴影后可酌情降低其 opacity 避免双重变暗——留作调参项。

### 9.3 P1 — 补一个冷色 Hemisphere（或等价环境调匀）

Reference 的暗部是冷蓝灰、明暗过渡柔和，当前暗部发闷（只有 IBL 撑）。`HemisphereLight(#3a4660, #14171c, 0.5)` 一行即可，成本可忽略；注意与 9.1 的总量平衡（先降 Directional 再加 Hemisphere，避免加回过曝）。

### 9.4 P2 — Environment 替代

RoomEnvironment 的室内灯箱感偏"棚拍"。P2 可换自定义 gradient/environment 场景（顶冷白、底深灰蓝），获得路面的竖向渐变反射。当前 0.04 模糊半径合理。

---

## 10. Postprocessing

现状：无任何后期链（直接 renderer 输出）。

### 10.1 P1 — Bloom（修正前置条件：先解决"让谁发光"）

Reference 中充电桩、节点点、独占区边框、车顶灯都有克制的辉光。**修正 v1 的方案漏洞**：当前节点、充电桩、呼吸灯全部是 **MeshBasicMaterial 的 LDR 颜色**（无 emissive、无 HDR 超亮值），ACES 输出后亮度普遍 < 0.9——直接上 `threshold 0.85` 的 UnrealBloom，**大概率只有白色文字（标签、P 字）和高光边缘被泛光，节点与充电桩根本不会发光**，效果与意图相反。
正确路径按成本升序：
1. **选择性 Bloom**（分层渲染明亮物体层再合成，不动主链材质）——实现复杂度中，且需挂入 TASK-016 资源代重建链；
2. **先给目标元素做 HDR 化**（充电桩/呼吸灯/节点描边改 emissive 材质或颜色乘 >1 的 HDR 值，配合 `toneMapped` 控制ACES 压缩），再上全局 Bloom（threshold 0.85）——效果最贴近 Reference，但材质改动面大；
3. Bloom 参数本身（strength 0.25~0.35, radius 0.6）是次要的。
**结论**：Bloom 列为 P1，但标注**前置依赖 = 发光元素设计（材质 HDR 化或选择性 Bloom 选型）**，不是"接一个 pass"就完事。若不做前置，宁可不加。

### 10.2 P2 — SSAO / Contact Shadow

4291 实例节点 + 合批贴花的场景，SSAO（GTAO/N8AO）收益集中在车辆与货架底部，成本高。已有的车辆假阴影 + 9.2 的实时阴影（启用后）覆盖大部分观感。**建议不做**。

---

## 11. Color System（色彩系统）

Reference 实测采样（`docs/prototypes/agv-3d-scene-prototype.png` 逐点取样）vs 当前常量：

| 语义 | Reference | 当前 | 差距 |
|---|---|---|---|
| 页面背景 | `#161b22`（暗蓝灰，非纯黑） | `#0c0e12` | P1 提亮 |
| 地面 | `#242931` | `#16191f` | P1 提亮 |
| 路面 | `#494c54`（比地面亮） | `#363c45` | 随 9.1 微调 |
| 独占区填充 | `#37465e` opacity~0.5 | 无填充（边 `#4f8dff`×0.3） | P1（需 8.1 算法前置） |
| 节点 work | 青绿（降饱和） | `#3fb3a4` | 随 5.1 微调 |
| 停车 P | `#59429a` | `#b07af5`（垫） | 色相接近，P2 |
| 车体状态色 | 高饱和红/蓝/黄/绿 | 同色表但受光后被过曝洗白 | P0（9.1 解决） |
| 标签底 | 深灰黑 `#1a1f26` + 白字 + 状态点 | 状态色 ×0.72 满底 | P0（圆点需 shader/图集工作，见 7.2） |
| 仓库垫 | 米/卡其 | `#e3cf7a`×0.32 | 色相接近；货架属 8.3 决策项 |

**主色结论**：Reference 的画面主色是**三级灰蓝（背景/地面/路面）+ 青色点缀（节点/充电）+ 状态色只出现在车与标签的小面积上**。当前实现把状态色与节点色铺满了大面积（蓝芯片、黄名称、青圆盘海），这是色彩"脏"和"闹"的根源。修复顺序：9.1 方向光降档（只影响受光材质）→ 7.1/7.2 标签 → 5.1 节点，色彩系统即自然归位；`NODE_COLORS`/`SHELL_STATE_COLORS` 两张色表本身不需要动。

---

## 12. 汇总清单（v2 修订版）

### P0 — 不解决就不可能接近 Reference
| # | 项 | 位置 | 动作 | 范畴 |
|---|---|---|---|---|
| P0-1 | 取景按包围球 → 地图高度占比 ~61% | `overviewFraming.ts` + `useCameraNavigation.ts:141` | 四角投影包络（**签名加 aspect**），目标：高度 ≥90%（宽随之 ~72%，见 1.1 定义） | 纯函数 + 一处调用 |
| P0-2 | fov 60 广角感 | `App.tsx:328` | fov → 25±5，联动初始 position / maxDistance | 一行参数 + 联动 |
| P0-3 | 尺度链倒置：节点 0.7m > 路 0.5m ≤ 车宽 0.7m | `mapAppearance.ts:52,58` | 路 →1.6~2.0m，节点 r →0.22~0.28 | 常量 |
| P0-4 | 路面接缝：弯道毛边 + **路口叠片** | `buildMapGeometry.ts:220-256` | 共享顶点 strip（路径内）+ **路径端点圆片端帽（路口）**；v1 的"只改 strip"不完整 | 几何构建 |
| P0-5 | 1185 个仓库名称地面文字海 | `LandmarksLayer.tsx` / `buildLandmarkData.ts` | 移除/收紧到近景辅助级 | 外观层 |
| P0-6 | 标签状态色满底（60 台 IDLE = 60 个蓝芯片） | `labelMaterials.ts:216` 等 | 深灰底 + 状态圆点（**需 shader SDF 或图集新单元格**），高度 0.6→0.45~0.5（= 车长 1.0~1.1×） | 小段 shader/图集开发 |
| P0-7 | 受光材质过曝洗白 | `mapAppearance.ts:71` | 方向光 2.2→1.1~1.3；**不动 exposure**（会误伤 Unlit 层） | 常量 |
| P0-8 | **阴影从未启用**（Canvas 无 `shadows`） | `App.tsx` + `MapVisualizationFeature.tsx:283-317` | 先启用（PCFSoft 默认），再按**灯光空间地图四角**收紧阴影相机（现正方形覆盖 311m，仅减 margin 只到 279m，无效） | 参数 + 一处 prop |

### P1 — 决定最终视觉质量
| # | 项 | 位置 | 动作 | 范畴 |
|---|---|---|---|---|
| P1-1 | 地面/背景提亮至 Reference 色域 | `mapAppearance.ts:24,27` | `#14181f` / `#1e232b` | 常量 |
| P1-2 | 网格 5m 方格纸感 | `GroundLayer.tsx` | 压暗对比或 Canvas 细纹贴图 | 常量/材质 |
| P1-3 | 黑色孤岛 | `mapAppearance.ts:32` | 边距 40~60m + FogExp2 | 常量 + 一行 fog |
| P1-4 | 路缘线 + 虚线中线 | `buildMapGeometry.ts` | 边线顶点色/副 LineSegments + 断线 | 几何构建 |
| P1-5 | 节点总览剔除（<2.5px 淡出） | `NodesLayer.tsx` | **shader LOD**（v1 的逐帧矩阵方案破坏静态缓冲设计，已否决）；需质量能力接线 | shader 注入 + 能力接线 |
| P1-6 | 车辆程序化细节：车轮、深色底围、载货纸箱 | `createVehicleGeometry.ts` | +1~2 个 part kind | 几何构建 |
| P1-7 | 独占区面填充 + 亮边 | `buildExclusiveGroupsGeometry.ts` | **需新增区域算法**（AABB/凸包/并集，见 8.1），非"补一个 Mesh"；形状贴合度要求先经产品确认 | 新几何算法（前置决策） |
| P1-8 | 交通锁表达增强（**不放大业务形状**） | `trafficGeometry.ts` | 抬升 0.2m + 边框亮线 + opacity 0.5 + 「已锁定/申请中」小字（图集复用） | 几何/材质 + 图集 |
| P1-9 | 冷色 Hemisphere 光 | `MapVisualizationFeature` | `Hemisphere(0.5)` 一行 | 一行 |
| P1-10 | 阴影相机按灯光空间四角收紧 + 柔化选型 | `MapVisualizationFeature.tsx` | 依赖 P0-8 启用；PCFSoft 先行，VSM 仅在偏硬时评估（`radius` 在 PCFSoft 下无效） | 调参 |
| P1-11 | Bloom（低强度） | 新后期链 | **前置 = 发光元素 HDR 化或选择性 Bloom 选型**（当前 LDR 颜色过不了 threshold，只会照白字）；需挂入 contextGeneration 重建链 | 特性开发（前置决策） |
| P1-12 | 总览标签只留 selected+FAULT | `labelLod.ts` 过滤一处 | 秩 0/1 白名单 | 一处过滤 |

### P2 — 锦上添花
| # | 项 | 说明 |
|---|---|---|
| P2-1 | 充电桩闪电贴花 + 底环脉冲增强 | 复用图集机制；**须与 59 处密度联动总览 LOD**（见 8.4） |
| P2-2 | 停车 P slab 凸起 + 微光 | 抬 3~5cm |
| P2-3 | 节点暗描边 | 贴图或内环几何 |
| P2-4 | SSAO | 建议放弃，收益/成本差 |
| P2-5 | 自定义 Environment（顶冷底暗渐变） | 替代 RoomEnvironment |
| P2-6 | 背景渐变/暗角 | 后期或大球面贴图 |
| P2-7 | 车行方向细箭头贴花 | 替代现有三角贴片 |

### 前置决策项（超出本次"纯视觉修改"边界，不进实施清单）
| 项 | 说明 |
|---|---|
| 仓库货架/仓库区 | 1185 个站点需先做**语义聚合**（区域聚类或独立 POI 配置）才有"仓库区"形态；逐节点放货架 = 1185 组模型海（见 8.3）。推荐本次只做名称移除 + 垛配色（8.3 方案 A） |
| 独占区形状贴合度 | 面填充的算法选型（AABB/凸包/凹包/并集）取决于产品对形状贴合度的要求（见 8.1） |
| 图例面板 | Reference 右下角图例属 DOM 覆盖层，SPEC §7.1 明确排除——**产品范围外，主动忽略**，不列入视觉差距 |
| 交通锁文字 | 面板内「已锁定/申请中」文字虽是视觉项，但引入新图集内容，与 P1-8 一起评估 |

### 实施边界（自我约束，v2 修订）
- **纯外观/低风险动作**落在：`mapAppearance.ts` / `fleetAppearance.ts` 两张常量表、`overviewFraming.ts` 纯函数（**签名 + 一处调用同步改**）、`buildMapGeometry.ts` / `buildLandmarkData` 几何构建、各 Layer 组件的外观段、`labelMaterials.ts` 着色器常量、`App.tsx` 一行 camera 参数 + `shadows` prop。
- **小特性级动作**（工作量 > 常量修改，需按既有架构接线）：标签圆点 shader/图集（P0-6）、节点 shader LOD（P1-5）、阴影启用与相机收紧（P0-8/P1-10）、Bloom 链（P1-11）。
- **不触碰**：数据源/运行时/仿真内核/协议适配/上下文恢复与质量分级的既有语义；**不新增**任何 Reference 中不存在的 UI（图例面板明确除外）。

---

## 13. P1 实施记录（2026-09-02）

> P0 全项已在先行提交落地；本节记录 P1 各项的实施结果与实机验证结论。
> 验证口径：610 项单元测试 / typecheck / oxlint / 架构边界检查全部通过；
> `pnpm dev` 实机截图核对总览、中景、近景三个层级。

| # | 结果 | 实施说明 |
|---|---|---|
| P1-1 | ✅ | `MAP_CLEAR_COLOR #0c0e12→#14181f`、`GROUND_COLOR #16191f→#1e232b`，实机确认「路 > 地 > 背景」明度阶梯恢复 |
| P1-2 | ✅ | 5m 方格刻线改为 **5m 平铺 Canvas 贴图**（1m 细线 ×0.93 + 5m 分缝 ×0.86 乘色）；总览被 mipmap 均化、近景露出细纹。Canvas 不可用时降级纯色（无头测试环境安全） |
| P1-3 | ✅ | 边距 10→**90m**（建议 40~60m，实机验证 50m 时菱形四角仍露背景，故加大）；`FogExp2` 密度 = **0.25/对角线**，雾色 = 清屏底色，「黑色孤岛」消除 |
| P1-4 | ⚠️ 部分 | **保留**：中线虚线化（1m 实/0.6m 空，相位跨关节连续）、路面提亮 `#363c45→#3f444d`。**放弃**：路缘暗色边线——实机验证发现路口处各路径边线交叉/断头形成杂乱「花瓣」轮廓（提亮压暗、端头收缩两版均不理想），且路面与地面的明度对比本身已提供 Reference 的铺装板边界，收益不抵成本 |
| P1-5 | ✅ | `createNodeLodMaterial`：顶点着色器推导投影直径，`[1.5, 3.5]px` 区间 GPU 侧淡出；静态实例缓冲零改写，视口高度经 uniform 低频写入。实机确认总览点毯消失、路网骨架可读 |
| P1-6 | ✅ | 新增 **2 个部件**：`wheels`（四轮合并几何，固定真实尺寸同信标模式）、`cargo`（托盘上两只纸箱，loaded 时显示）；底盘加高 0.05→**0.09m** 充当深色底围。批次 Draw Call 7→**9**（SPEC §6.3 预算 ≤8 的偏差系视觉分析授权的 +2 部件，已在本表记录） |
| P1-7 | ✅ | 按路线 2 实施：成员物理路径采样点**凸包** + 沿边外扩 1.2m 面填充（`#37465e` × 0.5，`EXCLUSIVE_FILL_Y` 位于外沿之上、路面之下）。当前地图独占区均为细长走廊形，凸包贴合度高；共线退化降级为包围矩形 |
| P1-8 | ✅ | 面板抬升至 **0.2m**、透明度 0.4→**0.5**、边缘亮色描边条带（同材质顶点色 ×1.6）、面板中央「已锁定/申请中」文字贴花（静态两格 Canvas 图集，无 Canvas 环境降级为无文字）；业务矩形形状与尺寸零改动 |
| P1-9 | ✅ | `HemisphereLight(#3a4660, #14171c, 0.5)`，与方向光同代创建/释放 |
| P1-10 | ✅（随 P0） | 阴影相机按灯光空间四角收紧（`LIGHT_SHADOW_MARGIN_M=6`）与 PCFSoft 已随 P0-8 落地，本项无额外工作；VSM 未启用（PCFSoft 观感已可接受） |
| P1-11 | ❌ 未实施 | 前置决策未决（发光元素 HDR 化 vs 选择性 Bloom 选型，且需挂入 contextGeneration 重建链）；按本报告「若不做前置，宁可不加」执行，留作后续特性立项 |
| P1-12 | ✅ | `FAR_IMPORTANT_MAX_RANK = 1`（`isFarImportantRank` 类型谓词）：总览档（投影 <8px）只保留 selected + FAULT，秩体系仍服务远景截断排序 |

**实机验证补充**：
- 总览：节点点毯与 5m 方格纸消失、地坪铺满画面且远角经雾融入背景、独占区蓝色走廊清晰、标签芯片收敛为告警本体；
- 近景：虚线中线、载货纸箱、深色底围、标签（深灰底 + 白字 + 状态芯片 + 电量条）均符合预期；路缘边线花瓣问题为本次放弃依据（见 P1-4）；
- 车体饱和度仍受 RoomEnvironment IBL 影响偏浅（P2-5 自定义 Environment 范围），未在本次处理。

---

## 14. P2 实施记录（2026-09-02）

> P2 各项在 P0/P1 之后落地；验证口径同 §13：617 项单元测试 / typecheck / oxlint /
> 架构边界检查全部通过；`pnpm dev` 实机截图核对总览、中景、近景三个层级
> （证据归档 `gui-test-screenshots/p2-visual/`）。

| # | 结果 | 实施说明 |
|---|---|---|
| P2-1 | ✅ | **闪电贴花**：单格 Canvas 图集（`chargeBolt.ts`，青色闪电 + 深描边）+ 桩身四立面贴花四边形合批（`renderOrder` 高于立柱，Canvas 不可用整体降级不创建）；**底环脉冲**：底环材质复合「LOD 淡出 + 正弦亮度脉冲」双注入（`createChargeFadePulseMaterial`），与灯球同周期（2.4s）、最暗 0.55（环为弱陪衬）；**总览 LOD**（8.4 密度联动）：立柱/光环/贴花共享投影尺寸淡出（世界尺寸 = 桩高 0.9m，[2.5, 7]px 区间平滑淡出）——总览 59 处成排立柱渐隐、中景完整呈现，实机确认总览无成排发光体、近景桩身闪电清晰 |
| P2-2 | ✅ | 停车点由贴地方垫改为**凸起 slab**（BoxGeometry 抬升 4cm，透明度 0.32 → 0.5）+ **微光光晕**（足迹 ×1.35 的加法混合贴面，opacity 0.14，抬升至 slab 顶之上）；`buildLandmarkData` 合同拆分为 warehousePad*（贴地垫不变）与 parkSlab*/parkHalo*（非等比缩放矩阵），实机确认紫色 slab 侧沿立体感明确 |
| P2-3 | ✅ | 节点盘改为「中心盘 + 暗描边内环」合并索引几何（`nodeDiscGeometry.ts`）：描边宽 0.04m，顶点色 = 描边乘数 0.22 × 实例色（保持节点色相的深描边），材质开 `vertexColors`；单 InstancedMesh 不变（仍 1 Draw Call），LOD 淡出与描边共存。实机确认近/中景节点呈「嵌 into 路面」轮廓 |
| P2-4 | ❌ 放弃 | 按本报告 10.2 结论执行：4291 实例 + 合批贴花场景下 SSAO 收益集中在车辆底部，已有假阴影 + P0-8 实时阴影覆盖，收益/成本差不成立 |
| P2-5 | ✅ | `createSceneEnvironment.ts` 以**顶点色渐变球**（天顶冷白 `#c9d7e8` → 地平灰蓝 `#3a4250` → 天底深灰蓝 `#161a21`，幂曲线 ^0.6）替代 RoomEnvironment 经 PMREM 预滤波；工厂更名 `createGradientEnvironment`（签名与 dispose 合同不变，TASK-016 重建链零改动）。P1 遗留「车体饱和度偏浅」随 IBL 整体降档回血 |
| P2-6 | ✅ | `createBackgroundGradient.ts`：512px Canvas 背景（顶部 `#181d26` → 底部 `#10141b` 线性渐变 + 径向暗角 0.3 向黑压边缘）挂 `scene.background`；Canvas 不可用降级为 `MAP_CLEAR_COLOR` 纯色（测试环境走降级分支，`<color>` 兜底保留）。雾色仍为清屏底色，总览远角渐隐衔接自然 |
| P2-7 | ✅ | 方向楔三角棱柱改为**带尾部中央凹口的细长箭头棱柱**（「➤」轮廓：鼻尖 +x、两翼全宽、凹口 −0.15）：俯视即可读出车行方向，保持原部件槽位/实例色/阴影语义（9 部件、Draw Call 不变）。实机确认多台车箭头鼻尖指向清晰可辨 |

**实机验证补充**：
- 总览：背景渐变 + 四角暗角生效（纯黑虚空感消除）、充电桩成排发光体按 LOD 渐隐、告警标签（selected + FAULT）保留；
- 中景：节点描边可辨、充电桩与光环恢复可见（LOD 阈值内）、交通锁红/黄面板与描边正常；
- 近景：桩身闪电贴花、紫色凸起停车 slab、节点暗描边、车辆 ➤ 方向箭头、虚线中线全部符合预期；
- 遗留观察：背景整体仍偏暗（ACES + 暗角叠加），如需再提亮可上调 `BACKGROUND_TOP_COLOR` 或降低 `BACKGROUND_VIGNETTE_STRENGTH`，属后续调参项。
